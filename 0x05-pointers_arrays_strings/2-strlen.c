#include "main.h"
/**
 *_strlen-returns the length of the string
 *@s:chain
 *Return:chain length
 */
int _strlen(char *s)
{
int variable;

for (variable = 0; s[variable] != '\0'; variable++)
{
}
return (variable);
}
